input {
  generator {}
}
output {
    elasticsearch {
      hosts => "https://es01:9200"
      index => "teste"
      user => "elastic"
      password => "xdr123"
      ssl_verification_mode => "none"
    }
}